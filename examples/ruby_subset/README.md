# Ruby subset lexer

This small specification demonstrates that constants, `%w` arrays, a heredoc
inside an action, and ordinary Ruby string interpolation can remain in the
source while the DSL calls are replaced by generated tables.
